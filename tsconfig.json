{
  "compilerOptions": {
    "target": "ES2020" /* Un estándar relativamente moderno y bien soportado. */,
    "module": "commonjs" /* Módulos CommonJS, compatibles con Node.js. */,
    "lib": [
      "ES2020",
      "DOM"
    ] /* Incluye las librerías base necesarias. Ajusta según tus necesidades. */,
    "outDir": "./dist" /* Directorio de salida para el código compilado. */,
    "esModuleInterop": true /* Facilita la importación de módulos CommonJS. */,
    "forceConsistentCasingInFileNames": true /* Evita problemas de mayúsculas/minúsculas en ambientes Unix. */,
    "strict": true /* Activa el modo estricto de TypeScript para mayor calidad del código. */,
    "skipLibCheck": true /* Omite chequeos de definición en librerías externas, reduce errores innecesarios. */
  },
  "include": ["src"] /* Incluye tu carpeta de código fuente. */,
  "exclude": ["node_modules"] /* Excluye dependencias. */
}
